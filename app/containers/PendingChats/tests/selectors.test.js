// import { selectPendingChatsDomain } from '../selectors';

describe('selectPendingChatsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
