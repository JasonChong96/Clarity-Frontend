// import { selectPatientLoginDomain } from '../selectors';

describe('selectPatientLoginDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
